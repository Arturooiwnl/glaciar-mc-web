---
import {STAFFS} from '../data/staff'
---

<section id="staff" class="relative flex flex-col px-6 sm:px-10 py-20 scroll-mt-36 justify-center items-center text-center">

    <header class="hero flex flex-col items-center justify-center dark:text-gray-300 text-gray-800 mb-10">
      <div class="logo mb-5">
        <h1 class="text-4xl sm:text-5xl font-Chakra font-bold bg-gradient-to-r from-[#7ADBF7] via-[#4AB5E4] to-[#2A6BA9] bg-clip-text text-transparent">Conoce al Staff</h1>
      </div>
    </header>
  
    <div class="mb-10 max-w-3xl">
      <h2 class="text-2xl font-bold text-gray-800 dark:text-white font-Chakra mb-3">Una experiencia única</h2>
      <p class="text-gray-700 dark:text-gray-300 leading-relaxed [&>strong]:text-sky-400">
        El <strong>Staff</strong> de <strong>GlaciarMC</strong> es completo, cubre todas las áreas para ser de <strong>utilidad para todos ustedes</strong>.
      </p>
    </div>
  
    <div aria-label="Miembro del Staff" class="max-w-6xl w-full grid grid-cols-1 sm:grid-cols-3 gap-6 z-10">
        {
            STAFFS.map((staff, index) => (
            <article class={`staff-el-${index+1} animate-delay-${100 * (index + 1)} group relative flex flex-col justify-center items-center`}>
            <span class="text-gray-50 font-NickMC text-lg sm:text-2xl bg-black/50 px-2 py-1 opacity-100 sm:opacity-0 group-hover:opacity-100 transition-all duration-300 rounded-sm">{staff.name}</span>
                <img height="200px" width="auto" class="mask-b-from-20% mask-b-to-80% group-hover:scale-105 transition-all duration-300" src={`/images/${staff.image}`}  alt={`Imagén del staff ${staff.name} de GlaciarMC`}/>
           
                <div class="border border-gray-600/90 transition-all duration-300 group-hover:border-gray-600/80 absolute bottom-10 flex gap-2 justify-center items-center bg-gradient-to-b from-sky-900 to-sky-500 w-[210px] h-20 rounded-2xl overflow-hidden">
                    <staff.rankIcon class="size-10"/>
                    <span class="font-Chakra text-2xl mt-1">{staff.rank}</span>
                </div>
            </article>
            ))
          }
    </div>
  </section>